<% if user_signed_in?%> 
<div class="main1">
  <div class="top1">ログインユーザー：『<%= current_user.nickname %>』
    <h1 class="nickname">
      診断結果！
    </h1>
  </div>
  <div class="top2">家賃：<%= current_user.rent %>円 
    <h1 class="rent">
      <%if current_user.rent <= 50000 %>
        <%= "安っ！事故物件ですか？それか都心の外れか……。"%>
      <%elsif current_user.rent > 50000 && current_user.rent <= 70000%>
        <%= "このくらいの額の家賃が理想的です！"%>
      <%elsif current_user.rent > 70000 && current_user.rent <= 90000%>
        <%= "中級貴族といった所か・・・。"%>
      <%else%>
        <%="論外。バカか"%>
      <% end %>
    </h1>
  </div>
  <div class="top3">光熱費：<%= current_user.utility_costs%>円 
    <h1 class="utility">
      <%if current_user.utility_costs <= 5000 %>
        <%= "安っ！仕事し過ぎじゃないですか？"%>
      <%elsif current_user.utility_costs > 5000 && current_user.utility_costs <= 7000%>
        <%= "倹約家ですね。あなたの努力いつか報われます様に"%>
      <%elsif current_user.utility_costs > 7000 && current_user.utility_costs <= 10000%>
        <%= "少し努力が必要ですね。電力・ガス会社を変えてみるのも一つです"%>
      <%else%>
        <%="いつか痛い目に合うぞお前"%>
      <% end %>
    </h1>
  </div>
  <div class="top4">携帯料金：<%= current_user.phone%>円 
    <h1 class="phone">
      <%if current_user.phone <= 8000 %>
        <%= "安っ！素晴らしい！！！"%>
      <%elsif current_user.phone > 8000 && current_user.phone <= 10000%>
        <%= "良いですね！不要なオプションは解約住んでますか？"%>
      <%elsif current_user.phone > 10000 && current_user.phone <= 13000%>
        <%= "不要なオプションの解約・料金プランの見直しをしましょう"%>
      <%else%>
        <%="お前みたいなバカがこの日本を支えてます。ありがとう"%>
      <% end %>
    </h1>
  </div>
  <h1 class="top5">
    <div class="grid-6">
      <%= link_to "＊[残りのデータも入力]＊", new_salary_path, class: "post" %>
    </div>
  </h1>
</div>
<% else %>
  <div class="main">
    <div class="header">
      <div class="hheder">
        <h1 class="h1">LCCへようこそ！！！</h1>
          <p class="bun">このアプリケーションは給与に対しての生活コストの最適化を管理するアプリです</p>
      </div>
    </div>
    <div class="center">
      <h1 class="dai"> ＊こんな経験ありませんか？＊ </h1>
        <div class="info">
          <%= link_to image_tag("lco.jpeg", class:"nageki"), "#" %>
          <div class="list">
            <ul>・今月の生活費が足りない</ul>
            <ul>・なぜか今月生活が辛い</ul>
            <ul>・不明な出費が嵩んで生活を圧迫している</ul>
            <ul>・貯金がうまくできない</ul>
          </div>
        </div>
      <h1 class="dai2">『原因は生活費を管理していないから』</h1>
      <h1>このアプリでは</h1>
        <div class="list2">
          <a class="kane">『給与』</a>
          <a>ー<a>
          <a class="kane">固定費</a>
          <a>(家賃・光熱費・交通費)<a>
          <a>＋<a>
          <a class="kane">変動費</a>
          <a>(食費・生活用品・雑費)<a>
        </div>
      <h1 class="saigo">これらを自動で計算し、生活コストの最適化を提案します！</h1>
    </div>
    <div class="footer">
      <h1 class="fot">＊まずは試しにやってみよう！＊</h1>
        <form>
          <a class="fon">給与</a>
          <input type="text">
          <a class="fon"> 固定費</a>
          <input type="text"> </a>
          <a class="fon"> 変動費</a>
          <input type="text">
          <input type="submit" value="計算してみる" class="submit">
        </form>
      <h1 class="botton">＊会員登録がお済みでない方は利用できない場合があります＊</h1>
    </div>
  </div>
<% end %>
